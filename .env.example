# Discord webhook URL that contact-form submissions are POSTed to (server-only).
# Local dev: copy this file to `.env` and fill in the value.
# Production (Cloudflare): set it as a Worker secret instead of committing it:
#   npx wrangler secret put DISCORD_WEBHOOK_URL
DISCORD_WEBHOOK_URL=
